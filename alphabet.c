# devipriya
#include<stdio.h>
int main()
{
char c;
printf("Enter the character: ");
scanf("%c",&c);
if((c>='a'&&c<='z')||(C>='A'&&c<='Z'))
printf("%c is alphabet. ",c);
else
printf("%c is not an alphabet. ",c);
return 0;
}
